J, S = "aA", "aAAbbbb"


def func(S):
    # num_jewels = 0
    # for i in range(0, len(S)):
    #     if J.find(S[i]) > -1:
    #         num_jewels += 1
            
    # return num_jewels
    for i in S:
        print(J.find(i))
        # if J.find()


func(S)
